network:
  version: 2
  ethernets:
    ens192:
      dhcp4: false
      addresses:
        - ${ip}/24
      gateway4: ${gateway}
      nameservers:
        addresses:
          - ${dns}
local-hostname: ${hostname}
instance-id: ${hostname}